(function (window, angular) {
    "use strict";

    angular.module('module.channel-admin')
        .controller('channel-admin.MainController', [
            '$scope',
            '$state',
            function ($scope, $state) {

            }
        ]);

})(window, window.angular);