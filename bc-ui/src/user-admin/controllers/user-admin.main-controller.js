(function (window, angular) {
    "use strict";

    angular.module('module.user-admin')
        .controller('user-admin.MainController', [
            '$scope',
            '$state',
            function ($scope, $state) {

            }
        ]);

})(window, window.angular);