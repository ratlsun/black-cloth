box: wercker/nodejs

build:
    steps:
        - script:
            name: permission to npm 
            code: sudo chown -R `whoami` /usr/local
        - npm-install:
            options: -g bower
        - npm-install:
            options: -g gulp
        - script:
            name: build with dependencies
            code: |
                cd bc-ui
                npm install -q
                bower install -q
                gulp prod



