box: wercker/nodejs

build:
    steps:
        - npm-install:
            options: -g bower
        - npm-install:
            options: -g gulp
        - npm-install:
            cwd: bc-ui/
        - script:
            name: bower-install
            code: bower install -q


